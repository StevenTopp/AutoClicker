# 智能后台鼠标连点器特性说明文档 (dev 分支增强版)

欢迎使用全新升级的 **“智能后台鼠标连点器”**！此版本已在 `dev` 分支上完成了全面的功能重构与视觉品质提升，提供了更加极客、人性化且高颜值的多点后台连点操作流。

---

## 🎨 1. 卡通可爱应用图标更换
*   **EXE 级别图标完美显示**：编译生成的 `AutoClicker.exe` 在 Windows 资源管理器、系统任务栏、以及窗口左上角均采用全新的精美卡通鼠标图标进行高分辨率渲染，告别了默认的空白程序图标，极具辨识度。
*   **网页 Header 同步**：网页顶部的静态 Logo 元素同步替换为该可爱图标，使程序混合界面浑然一体。

---

## 🔴 2. 屏幕已标记点位红圈指示器 (默认 Option 2)
为了让用户清晰掌握已捕获点位在屏幕上的精准位置，程序推出了屏幕指示器功能：
*   **默认 Option 2 (空心高精红环)**：以半透明红底配合深红外环，并在中心保留一个高对比度定位准心。既美观醒目，又绝不遮挡底部任何软件的文本或按钮。
*   **物理鼠标 100% 穿透 (Zero-Block)**：指示器采用 WPF 顶层透明非激活窗口，在底层无缝应用了 Win32 `WS_EX_TRANSPARENT` 特性。您的物理鼠标可以直接穿透红圈，正常操作底下的任意软件，毫无任何卡顿或阻碍。
*   **高精数字序号渲染**：每个圆圈中心均以粗体渲染其对应的数字标号（`1`, `2`, `3`...）。
*   **数字标号动态重排**：当您在列表中删除、插入或变更点位时，屏幕红圈内的数字标号会自动进行主线程重排，永远与软件界面列表的 `index + 1` 保持绝对完美一致。

---

## ⌨️ 3. 全新全局快捷键控制流程
全新的快捷按键逻辑更适合复杂的多点高频连点录入，操作流程如下：

1.  **开始/结束采点模式**：按下 **`Ctrl + Alt + F7`** (全局热键)
    *   主应用窗口会自动最小化，腾出屏幕空间。
    *   状态栏切换至“采点中”蓝色发光呼吸灯。
2.  **悬停并记录坐标**：将鼠标移动到任意目标软件的连点位置，按下 **`F7`** (全局热键)
    *   自动记录当前鼠标下的屏幕绝对坐标、窗口相对坐标和顶级窗口句柄。
    *   **支持多次按 `F7` 键**：悬停到哪里，按一下 `F7` 即可在当前位置新增一个标记点，屏幕上会自动即时浮现对应的数字红圈指示器！
3.  **启动/暂停连点任务**：按下 **`F8`** (全局热键)
    *   单键双态（Toggle）控制：运行中按 `F8` 自动暂停，静止中按 `F8` 自动恢复点击。
    *   如果处于“采点中”状态，按下 `F8` 会自动强制退出采点状态并开启连点。
4.  **清空全部标记坐标**：按下 **`Ctrl + Alt + F9`** (全局热键)
    *   一键将列表全部清空，并即时彻底销毁屏幕上的所有红圈指示器。

---

## 🚫 4. 点位独立禁用控制 (跳过连点且不消失)
现在，您可以对列表中的任何一个坐标点进行独立的状态控制，而不需要将其删除：
*   **状态切换**：在列表卡片左侧点击“眼睛/电源”开关，或使用快捷键，即可一键将该点“启用”或“禁用”。
*   **淡化不消失视效**：
    *   当点位被**禁用**时，屏幕上的对应红色圆圈的**不透明度（`Opacity`）会即时降为 `0.25`**（显示为非常淡的半透明红，不消失，方便留作定位参考）。
    *   当点位被**启用**时，指示器不透明度瞬间恢复为 `1.0` 正常亮度。
*   **引擎秒级跳过**：不管是“顺序循环”还是“独立并发”多线程连点模式，一旦某个点被禁用，点击引擎会在点击循环中**直接跳过它**，完全不会影响其他点位的点击频率和连点任务。

---

## ⌨️ 5. 鼠标悬停快捷禁用/启用 (`Ctrl + Alt + D`)
为了省去在软件界面寻找卡片的繁琐，我们特别开发了这款快捷禁用手势：
*   **30像素欧氏距离锁定**：将物理鼠标移动到屏幕上任意红色圆圈附近（距离圆圈中心 30 像素以内），按下 **`Ctrl + Alt + D`** (全局热键)。
*   **一键 Toggle 并触发脉冲高亮**：
    *   C# 后端会自动定位距离鼠标最近的那个红圈，将其“启用/禁用”状态进行一键切换，并让红圈播放一次放大脉冲动画作为反馈！
    *   主软件界面的对应卡片也会**微幅震动缩放并同步置灰**，真正实现屏幕与 UI 的双向联动。

---

## ⚡ 6. 卡片悬停高亮动画 (Hover Pulse)
*   **鼠标移入悬停放大**：当您将鼠标光标移动到主程序界面的任意坐标点卡片上时，屏幕上对应的红色指示圈会**瞬间放大 1.35 倍并播放平滑的缩放脉冲高亮动画**！
*   **鼠标移出即时还原**：移出卡片时，圆圈自动还原。极大地方便了多点位场景下的快速视觉核对。

---

## ⚙️ 7. 毛玻璃拟态按键设置面板
*   **高颜值微光界面**：点击右上角齿轮，弹出的配置面板采用精致的玻璃拟态（Glassmorphism）与深色霓虹光环设计，极具科技感。
*   **快捷键录制功能**：只需用鼠标点击快捷键配置输入框，随后按下键盘上的任意键（支持 `Ctrl/Alt/Shift/Win` + 任意按键），输入框即可自动捕捉按键名称，完美录制并即时绑定。
*   **指示器样式切换**：支持在 Option 1 ~ Option 4 四大设计样式中实时自由切换，红圈效果立竿见影。
*   **数据持久化**：所有的按键配置和红圈样式都会通过 `localStorage` 自动持久化在您的本地缓存中，重新打开应用会自动回显，并在初始化时无缝同步绑定到 C# 系统的底层全局热键。

---

## 📁 8. 编译发布信息
*   **源码分支**：`dev` 特性开发分支
*   **本地 Release Standalone 独立版目录**：`D:\Code\projects\AutoClicker_Standalone`
    *   包含了免安装的独立运行包，双击 `AutoClicker.exe` 即可运行。
